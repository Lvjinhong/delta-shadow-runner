Delta External Vision Runner 开发进度
更新时间：2026-07-18
GitHub：https://github.com/Lvjinhong/delta-shadow-runner

[最终目标]
- 交付纯 Windows Python 脚本：桌面截图 → 视觉识别/定位 → 固定路线规划 → 标准键鼠输入 → 画面反馈与卡住恢复。
- 首个游戏 MVP 限定一个固定分辨率、一个地图区域、一个出生区域和一条固定路线。
- 不读取目标进程内存，不注入 DLL，不使用驱动，不修改游戏文件，不实现反作弊规避。
- 不实现敌人识别、自动瞄准、自动射击或战斗决策。

[2026-07-16 当前检查点]
- 早期 React/Node 模拟平台、REST/WebSocket、浏览器 Dashboard、npm/Vite/Playwright 工具链和旧启动入口已从当前工作树移除；历史仍可从 Git 找回。
- 删除前已把旧 TypeScript A* 的关键安全回归迁到 Python：未知节点、贪心陷阱、极端坐标、累计代价溢出、零权边、重复边、悬空边、非有限值和输入不可变。
- Python A* 新增 1000 轮固定种子随机图与参考 Dijkstra 差分；修复极端坐标下 `0 * inf` 产生 NaN 导致错误选路，以及累计代价溢出错误缺少具体边的问题。
- 新增多尺度 OpenCV 模板匹配：固定 ROI、NMS、阈值、空间歧义拒绝、跨 waypoint 歧义拒绝和路线坐标映射；同 waypoint 的多张外观模板先聚合，不会彼此制造假歧义。
- 新增可追溯模板 Profile：固定分辨率、命名 ROI、模板/manifest SHA-256、来源 run/sequence、路径逃逸保护、单次字节哈希与解码、无可用缩放比例拒绝。
- 新增人工路线 calibration/validation/blind 采样、流式标定和独立评估；拒绝 run、整帧和感知 ROI 泄漏，并强制 100% 标签覆盖。
- Worker 已接入 template profile schema v2、抽象路线位置、waypoint/A*、每条边一次相对鼠标转向和重复有界按键脉冲。
- replay 新增独立 input-events.jsonl；正常结束、无下一帧超时、异常退出和慢截图/watchdog 并发均保留输入顺序。
- ReplayFrameSource 已直接实现 Worker 的 grab/close 协议，同时保持独立迭代游标；标定 Profile → 独立 blind 评估（含负样本）→ schema v2 Worker → 动作事件回放的离线 E2E 已通过。
- 新增显式不可变 ObservationScope：首次定位明确使用全局模板；路线建立后正常候选只含 current/next 的有归属模板，主候选拒绝后才诊断其他已知节点并安全停机；空候选与无归属模板 fail closed，范围外颜色锚点同样触发安全停止。
- blind evaluator 显式标记 observation_scope=unconstrained_perception，不从真值标签构造候选集；在线路线约束只由完整路线 E2E 验证。
- 新增 Windows Preflight 一键入口：锁定依赖与配置只读校验、受控 SendInput E2E、5 秒切回游戏、60 秒前台截图基准和机器可判定报告串联执行；dry-run 也纳入单 Worker mutex。
- 截图基准新增 missing/near-black/foreground HWND 硬门槛并落盘 capture-gate.json；Preflight 会严格校验指标 schema/FPS 一致性、唯一 run_id、证据新鲜度、首末帧 SHA-256、事件时间与终态顺序、按键释放和独立 ground truth，即使产物缺失也保留失败报告。该链尚未在 Windows 可见交互桌面实跑。
- 独立评审实际复跑 9 个完整证据反例和 1 个缺参数反例，均未误报 PASS；有效基线仍通过。PowerShell 5.1 空格路径修复已完成源码复审。
- 本机完整 Python 回归：419 项测试通过，总覆盖率 88.48%，Ruff、sdist/wheel build 与独立 10-Gate 控制链验收通过。
- Windows 已同步到 aa94d18：uv 0.11.28、CPython 3.12.13 与锁定依赖安装成功；Windows 原生 419 项测试、88.48% 覆盖率、Ruff、sdist/wheel build 均通过。OpenCV 精确模板分数存在约 1.13e-6 的平台舍入差异，测试已改用 1e-5 严格容差，算法阈值未变。
- Windows 交互任务已实际启动受控 Tk 目标，但 console Session 1 处于锁屏（LogonUI）；DXcam 返回 0x80070005，MSS 对照返回 BitBlt 失败，均按失败处理。解锁桌面后需重跑，不能把这两次样本推成 backend 结构性不支持。

[已经具备]
- DXcam 0.3.0 主截图 backend、MSS 10.2.0 回退 backend。
- PNG/JSONL 录制、确定性 Replay、结构化事件与截图性能基准。
- 颜色锚点受控测试、模板锚点、waypoint/A*、非阻塞短脉冲和有限卡住恢复。
- dry-run 与 Win32 SendInput，完整窗口标题 + HWND 前台守卫、F12 急停、独立按键释放 watchdog。
- Windows 依赖安装、游戏路线工作流和一键入口：vision.ps1、start-game-route.cmd、start-controlled-e2e.cmd。
- vision.ps1 已提供 Sample、Calibrate、Evaluate、DryRun、Armed；schema v2 armed 默认由 armed_ready=false 阻断。
- GitHub/官方文档源码调研：docs/research/2026-07-15-external-vision-options.md。

[尚未完成，不能宣称通过]
- 尚无《三角洲行动》真实截图、独立 blind set、precision/recall/F1 或固定路线成功率。
- Windows 主机 192.168.3.7 在 2026-07-16 已恢复 ping/SSH；当前 console Session 1 锁屏，且 Windows 尚无 configs/game-route.json 与 profiles/route-01/templates.json。仍未完成可见桌面截图、SendInput、F12 <=200ms、受控路线 20/20 或游戏路线 E2E。
- feature-list.json 中相关功能继续保持 passes=false。

[下一步]
1. 解锁 Windows console Session 1 并保持桌面可见，重跑受控 E2E 与 60 秒 DXcam/MSS 基准。
2. 采集同一固定路线的 calibration 与独立 blind 运行，按 README 生成标签、Profile 和 metrics。
3. 先跑 game dry-run，再跑受控窗口 20/20、F12 <=200ms，最后在授权游戏场景跑固定路线 armed E2E。
4. 根据真实 blind metrics 与完整路线结果调整 ROI、模板、mouse_dx/dy 和状态机阈值，不凭单次成功宣称准确率。

[恢复命令]
- 本机：cd /Users/bytedance/.codex/tmp/delta-shadow-runner
- 同步依赖：uv sync --frozen --python 3.12
- 测试：uv run pytest -q
- 静态检查：uv run ruff check python python_tests
- 构建：uv build
- Windows：进入 D:\Work\Work-Project\delta-shadow-runner 后按 README 运行 vision.ps1；实际路线入口为 start-game-route.cmd，受控自检为 start-controlled-e2e.cmd。

[验收边界]
- 合成图、受控 Tk 窗口和离线回放只能证明管线行为，不能证明《三角洲行动》准确率。
- 实际准确率只能来自按完整 run 隔离的真实游戏 blind test；必须同时报告样本量、会话数、precision、recall、F1、false lock 和完整路线成功率。
- 如果标准桌面截图或 SendInput 被目标拒绝，记录为不支持，不升级到注入、驱动或规避方案。

[2026-07-16 停机前实机检查点]
- 用户已完成主页前的新手教程；随后实际处理了进入特勤处时出现的一次“提升建筑等级”说明浮层。
- 当前版本实测链路：主页/大厅 → 行前备战 → 战略板 → 点击“零号大坝”文字 → “零号大坝-常规”大厅 → 跳过配装 → 出发。
- 零购买方案已真实进图；出生画面仅持刀，位置在货运集装箱道路，路牌指向 Substation 200m 和 Administrative Area 188m。
- 进入约 17 秒、尚未开始路线移动时被其他玩家使用 SG552 击倒；本次只能证明零成本进图和一个出生样本，不能证明地图路线或成功率。
- `M` 按键已成功插入，但采集时已进入“最终倒地”页，因此不能记为战术地图打开成功。
- 结算页“下一步”空格已发送；用户随即要求断开主机，本次没有再读取当前画面。恢复时必须从新截图重新确认状态。
- 详细证据、Windows artifact 路径和恢复顺序见 `docs/checkpoints/2026-07-16-zero-cost-zero-dam.md`。

[2026-07-18 开发暂停检查点]
- 用户准备关闭本机并断开 Windows 主机，本轮只保存状态，没有继续开发，也没有向游戏发送键鼠输入。
- macOS/GitHub 当前提交为 `2eb57c964cb68c17b388ac1f706dbd638375aa96`；Windows 仍为 `ff7a38fa9402f74b600d1adc62dd06dc647becfc`，只缺检查点文档提交，运行源码一致。
- Windows 工作区仅有预期未跟踪运行产物 `artifacts/`、`node_modules/`、`playwright-report/`、`test-results/`，恢复时必须保留。
- 2026-07-18 macOS 基线重跑：434 项测试通过，总覆盖率 87.64%，Ruff、sdist/wheel build 通过。
- publickey-only SSH 健康检查通过；只读截图探针未找到 `三角洲行动  ` 游戏窗口，未生成新数据集，本轮没有任何输入动作。
- 持久目标保持进行中；大厅正式状态机、真实出生 blind 数据、ORB/SIFT A/B、固定短路线、保险箱/退出闭环和真实游戏成功率都未完成。
- 下次从新截图确认当前页面开始，先下载已有真实截图做离线 TDD，再实现 fail-closed 菜单状态机。
- 完整状态与恢复顺序见 `docs/checkpoints/2026-07-18-development-pause.md`。

[2026-07-18 恢复开发：截图菜单状态机]
- 新增独立 `menu_automation.py`，没有把页面状态伪装成路线 waypoint；页面流程和局内导航保持两个控制器。
- 页面分类固定期望分辨率，复用模板匹配阈值与歧义拒绝；页面锚点和动作锚点必须是不同 detector，动作锚点还必须落在显式允许点击 ROI。
- 零成本流程以 3 个严格递增、750ms 内、页面/动作锚点漂移不超过 12px 的一致帧确认；点击点使用三帧中位数。
- 每个页面动作只生成一次并锁存，必须等待目标页面重新确认；8 秒超时不重试，死亡页、旧帧、未来帧、乱序帧、低置信、歧义和分辨率变化均不产生动作。
- 每个动作意图携带绝对过期时间；下一阶段 actuator 接入必须在实际 SendInput 前再次校验，当前尚未发送真实点击。
- TDD 红灯先复现模块缺失和四类可构造绕过；最终定向 22 项、全量 456 项测试通过，总覆盖率 87.11%，Ruff 和 sdist/wheel build 通过。
- 独立 code-reviewer 最终无 CRITICAL/HIGH/MEDIUM/LOW finding，结论 APPROVE。
- Windows 首次恢复时 SSH 通过，随后主机离线；远端 artifact 尚未回收到本机，真实菜单模板、blind 指标和 live E2E 仍未完成，相关 feature passes 保持 false。

[2026-07-18 恢复开发：受保护的绝对点击]
- `win32_native.py` 已增加虚拟桌面绝对坐标映射与鼠标左键 down/up，四角、负虚拟原点、越界和类型错误均有单测；该阶段提交为 `2c6bbc7`。
- `Win32InputActuator.click_left_at` 只接受未过期的视觉动作；在绝对移动前和 mouse-down 紧邻位置分别复核 F12、前台 HWND/标题与动作过期时间。
- 绝对移动、相对移动、left-down/watchdog/left-up 共用同一鼠标事务锁，避免上一轮卡住时形成拖拽、并发点击坐标交错或相对移动改变点击落点。
- 鼠标释放不受前台/急停闸门阻止；首次释放失败会立即重试，独立 watchdog、`release_all` 与 `expire_overdue` 继续兜底。Timer 工厂构造或启动失败也会走立即释放路径。
- TDD 覆盖过期、失焦、卡住状态零移动、锁等待过期、并发串行、SendInput 失败、watchdog 构造/启动失败和键鼠释放失败组合；定向 39 项测试及 Ruff 通过。
- 独立 code-reviewer 逐项复现并推动修复三类并发/异常问题，最终无 finding，结论 APPROVE。尚未在 Windows 可见桌面发送真实鼠标输入，不能标记 live E2E 通过。

[2026-07-18 恢复开发：多帧路线确认与 ORB/SIFT]
- `VisualNavigationController` 新增初始 waypoint 3 帧、节点推进 2 帧、重定位 3 帧的连续确认；候选变化会清零，局部确认不会延长定位超时。
- 截图和观测的 sequence/captured_at 必须为严格递增的非负整数且逐帧一致；控制时钟必须为非负、单调整数，任何违约都先释放输入再停止。
- `FeatureWaypointObserver` 把 ORB/SIFT 的 KNN ratio test、RANSAC/inlier、重投影误差和投影四边形结果映射为路线节点；多个不同 waypoint 同时通过时保持 uncertain，不按 raw score 强选。
- 路线外模板只在当前/下一节点零命中后用于偏航诊断；当前/下一节点存在歧义时不会被路线外命中覆盖。该边界由先红后绿的回归测试固定。
- Profile 和 `vision.ps1 -Mode Calibrate` 支持 `-FeatureBackend ncc|orb|sift` 与 `-MaximumFeatures`；README 已补齐用同一 calibration/blind 数据做 NCC/SIFT A/B 的完整命令。
- 本机 Python 3.12.12 全量验证：653 项测试通过，总覆盖率 87.27%；Ruff、依赖冻结同步和 sdist/wheel build 通过；`pip-audit` 对锁定的运行时三方依赖未发现已知漏洞。
- 以上仍是离线实现证据。Windows 可见桌面、真实截图 blind 指标、受控窗口 20/20、游戏 dry-run/armed 路线和实际准确率尚未执行，相关 `passes` 保持 false。

[2026-07-18 恢复开发：大厅到局内的全会话闭环]
- 新增 `menu_worker.py`：持续截图、页面/按钮多帧确认、每个转换一次性执行、输入事件落盘、超时/异常安全清理；菜单终态为 `COMPLETED` 或 `STOPPED`，不会把未知页面当作可执行状态。
- 新增共享输入事件持久化和严格 JSONL 恢复：每个输入带稳定 `event_id`，replay/runtime 双端幂等；完整写后失败、半写坏尾行、跨实例旧游标和中间损坏 fail-closed 均通过故障注入。
- 新增 `game_session.py`：从 `LOBBY` 开始运行菜单 Profile，只有画面确认 `IN_MATCH` 才启动局内路线；菜单未完成时 route 严格为 null，两阶段共用 run_id，证据分别写入 menu/route，汇总写入 `session-summary.json`。
- 全会话强制 schema v2 `TemplateProfile`；菜单 Profile、路线 Profile 和窗口客户区分辨率必须一致。schema v1 ColorAnchor 和分辨率不一致在创建窗口、截图源或输入执行器前拒绝。
- `vision.ps1` 新增 `SessionArmed`；`start-game-route.cmd` 的 A 从大厅启动完整会话，D 保留为手动进图后的局内 dry-run；整个会话保持同一 `Local\\DeltaVisionWorker` mutex、显式 `-ConfirmArmed`、F12 和前台 HWND/标题闸门。
- README 和安全配置示例已补菜单 Profile bundle、validate-only、SessionArmed、artifact 结构及退出码说明；不附带假装可跨环境通用的真实游戏模板。
- 本机 Python 3.12.12 全量验证：679 项测试通过，总覆盖率 86.60%；Ruff、uv sync --frozen、sdist/wheel build、pip-audit 通过。菜单/JSONL 独立审查和全会话独立审查最终均 APPROVE。
- Windows `192.168.3.7` 当前 ping 丢包、TCP 22 超时、SSH 报 Host is down；PowerShell 5.1、可见桌面、菜单点击、固定路线和准确率尚未实跑，相关 feature passes 继续保持 false。

[2026-07-18 恢复开发：可移植菜单 Profile 交付包]
- 新增 `package_menu_profile.py`：先完整校验来源 Profile、run manifest、帧内容和模板哈希，只复制被引用的 6 个 calibration run 与 11 个模板资产，并从输出目录重新加载 `menu.json`。
- 目录与 ZIP 使用同文件系统临时产物和原子 no-replace 发布；并发出现同名 ZIP 或空目录会保留用户路径并失败回滚。半写 ZIP、发布失败、清理失败和发布前 `stat` 失败均有先红后绿的故障注入测试。
- 最终本机菜单包为 `output/windows-ready/menu-zero-cost-v3.zip`，44 MiB，SHA-256 `3b740125c29dafec1f6cf7356db9afa2d4f1137a271364a3bff6dab577e44230`；ZIP 完整性及解压后 Profile 重载通过，Profile 为 2560×1440、6 个来源 run、6 个场景模板。
- 本机 Python 3.12.12 全量验证：687 项测试通过，总覆盖率 86.21%；Ruff、sdist/wheel build、`git diff --check` 和锁定运行时依赖 `pip-audit` 通过；打包器历经三轮独立故障注入复审后最终 APPROVE。
- `game_session --validate-only` 已越过菜单包加载，并按预期停在缺失 `profiles/route-01/templates.json`。这说明大厅菜单数据已可交付，但局内路线 Profile 仍未采集，不能据此宣称跑图 E2E 或准确率。

[2026-07-19 基线切换：1920×1080 与进图即采样]
- 用户明确把产品拆成两层：外层必须循环完成主页/地图选择/零成本出发/赛后回大厅/背包与保险箱清理，内层才负责零号大坝固定路线；当前开发顺序改为先完成外层循环。
- 新增 `SessionSample`：只依赖菜单 Profile，不依赖尚未生成的路线 Profile；菜单必须以 `COMPLETED + IN_MATCH` 终态结束，随后立即释放菜单输入并以同一个 run_id 启动只读人工路线采样。菜单失败、错误终态或采样失败都会写原子摘要。
- 菜单运行参数从路线 Worker 抽为 `WindowsMenuSettings`；状态模型增加 `BASE`，按键白名单增加 `tab`，菜单结果记录真实 `terminal_scene`。`start-route-calibration.cmd` 仍要求人工确认和 F12 急停。
- 本机全量验证：698 项测试通过，总覆盖率 86.06%；Ruff、sdist/wheel build 与 `git diff --check` 通过。
- 2026-07-19 只读实测确认游戏客户区已改为 1920×1080 窗口模式；2 秒 MSS 采样 4/4 帧、无缺帧。旧 2560×1440 菜单包和全部模板/ROI/坐标从此视为过期，不能缩放复用。
- 当前 1080p 画面停在视频设置页；尚未在 1080p 基线上发送菜单输入，也尚未采集主页、仓库、保险箱、赛后和 HUD 的完整 Profile，相关真实循环与路线准确率继续视为未完成。

[2026-07-19 真实零成本首轮与外部循环编排]
- 只读复核确认游戏客户区稳定为 1920×1080，采集了干净主页数据集 `homepage-windowed-1080p-home-clean-20260719-01`；旧 2560×1440 Profile 继续禁止复用。
- 经独立 live 输入审查后，仅执行一次受保护的主页“出发”点击；审计通过且只产生 3 个鼠标事件，完成匹配、选干员、加载、零号大坝出生、倒地/死亡、赛季通行证页并自动返回主页。该轮没有局内移动，不能计为跑图成功样本。
- 新增 `passive_scene.py`：赛后阶段不持有任何输入接口，只读观察 `IN_MATCH/DEATH_SUMMARY/POST_MATCH`，连续确认 3 帧 `BASE` 才成功；未知、歧义、过期、乱序、分辨率变化、客户区移动/缩放、抓帧期间失焦都 fail-closed。
- 新增 `external_loop.py`：严格串行执行 `BASE 单击进图 → 局内 runner → 被动回厅 → 可选 cleanup`；上一轮未确认 `BASE_CONFIRMED + terminal_scene=BASE` 时绝不清理或开始下一轮。entry/match 强制同一窗口标题、同一 F12 急停键和同一 1920×1080 Profile。
- cleanup 证据区分 `cleanup_started` 与 `cleanup_completed`；抓帧和截图源关闭同时失败时保留原始安全错误并附加关闭错误，不让次要异常遮蔽根因。
- 本机 Python 3.12.12 全量验证：720 项测试通过，总覆盖率 85.81%；本次改动 Ruff、sdist/wheel build 与 `git diff --check` 通过；独立 code-reviewer 最终无 finding，结论 APPROVE。
- 仍未完成：1080p 正式菜单 Profile bundle、仓库/保险箱清理 hook、局内 HUD/出生点 blind 数据、零号大坝固定路线和真实完整跑刀成功率。

[2026-07-19 仓库空保险箱识别与无输入 DryRun]
- 菜单视觉模型新增 `WAREHOUSE` 场景和必需附加证据；页面模板只有在页面锚点与全部 evidence 同时通过时才会被接受，任一证据缺失都会返回 `UNKNOWN`。
- 菜单 Profile schema v2 专用于 evidence 配置：v1 拒绝非空 evidence，v2 必须至少包含一组 evidence；旧 v1 无 evidence Profile 保持兼容，防止旧读取器静默忽略安全字段。
- 仓库专用观察器只接受恰好两个固定模板和 `BASE → WAREHOUSE → BASE` 拓扑；空保险箱必须同时具备 `0/2` 计数、0 号空格、1 号空格三个固定且内容独立的 evidence。任何模板 ID、来源哈希、动作锚点或拓扑缺失都 fail-closed。
- 新增无操作系统输入的仓库 Worker 与 `DryRunCleanupExecutor`：动作过期或重复会拒绝，输出只记录本地/屏幕坐标；资源关闭采用 at-most-once 语义，`close()` 抛错也不会再次调用。
- 真实 1920×1080 calibration 数据生成 schema v2 Profile：`output/windows-ready/warehouse-empty-1080p-v3/menu.json`，2 个页面模板、2 个动作模板、3 个 evidence 资产，Profile SHA-256 `238ea1d17ec00be2dd98c0e282bb8c4c763b0cf80bbdaf43d337c09b316a9832`。
- 真实帧离线回放完成 9 帧、2 个 DryRun 动作，确认 `safe_box_count=0`、截图源关闭且没有输入事件文件；事件 SHA-256 `3b9a8c90a2b5dd8a72397ce9c4cb916b611b09b7241e8b9d49c4677834915028`。记录坐标为本地 `(322,55)/(192,55)`、屏幕 `(642,211)/(512,211)`，仅是回放证据，未发送真实游戏输入。
- 像素扰动复核：真实空仓原图接受；分别改变 `0/2` 计数、0 号格 30×30、1 号格 30×30 后均返回 `UNKNOWN`。这只能证明所构造反例被拒绝，不能替代独立 blind 集准确率。
- 本机 Python 3.12.12 全量验证：775 项测试通过，总覆盖率 85.13%；Ruff、sdist/wheel build 与 `git diff --check` 通过。独立 code-reviewer 复跑全量和定向测试，最终无 CRITICAL/HIGH/MEDIUM finding。
- Windows `192.168.3.7` 当前 SSH/TCP 22 超时，尚未同步本提交，也未执行仓库真实点击；外部循环 live E2E、局内出生 blind 数据、零号大坝固定路线、20/20、F12 急停和真实完整循环仍未完成，相关 feature `passes` 继续保持 false。

[2026-07-19 受保护的仓库清理运行链路]
- 仓库清理新增可注入外部循环的具体 Windows session；DryRun 不创建 Win32 输入链路，Armed 只允许进入仓库和返回主页两种鼠标动作，当前空保险箱策略明确拒绝任何格子转移动作。
- 鼠标专用执行器允许空键盘扫描码表，但所有键盘动作仍 fail-closed；Armed 运行同时要求显式 `armed_ready`、F12 未按下、精确前台 HWND/标题、动作未过期且未重复。
- 每次真实动作都会记录 `dry_run=false`，鼠标移动、按下、释放三类输入事件即时持久化；动作意图在发送前即被消费，部分输入失败只释放设备且不会重试同一意图。
- 独立 code-reviewer 故障注入发现同标题窗口可能形成 A 客户区与 B HWND 错绑；已改为只解析一次精确 HWND，并让路线、菜单和仓库三个真实输入 builder 都从同一个 HWND 派生客户区、截图源、坐标和 SafetyGate。
- 原 A-region+B-HWND 攻击重放已被阻断：handle 222 仅传入 region resolver 一次并选择 B 客户区，旧 A 坐标不可达；全仓库其余按标题取客户区的路径均无真实输入能力。
- 本机 Python 3.12.12 全量验证：793 项测试通过，总覆盖率 84.76%；Ruff、sdist/wheel build 与 `git diff --check` 通过。独立 code-reviewer 最终无 CRITICAL/HIGH/MEDIUM finding，结论 APPROVE。
- 当前产品基线继续固定为游戏客户区 1920×1080；窗口外框尺寸、桌面缩放或 UI 缩放不能替代客户区尺寸校验。Windows SSH 仍不可达，本阶段尚未向游戏发送仓库真实点击，也未翻转任何 live E2E feature pass。

[2026-07-19 完整外循环生产入口]
- 新增 `external_session.py`：从同一 schema v2 配置装配 `entry → match → 被动 return → warehouse cleanup`，并把具体 `WindowsWarehouseCleanupSession` 注入外部循环；生产 CLI 同时提供不依赖路线 Profile 的 `--cleanup-only`。
- 配置增加 `external_loop` 和 `warehouse_cleanup`；标题、截图 backend、F12、最大按键时长只从根配置派生，entry/match/cleanup 必须严格同源。仓库确认帧、漂移、帧龄和转换超时从经过校验的仓库 Profile 派生。
- Armed 完整循环必须同时满足路线和仓库两项 `armed_ready`，正式入口还要求 CMD Y/N、PowerShell `-ConfirmArmed` 和 Python `--confirm-armed`；任一缺失都会在 artifact、窗口解析或输入对象创建前拒绝。
- `vision.ps1` 新增 `LoopDryRun/LoopArmed/WarehouseDryRun/WarehouseArmed`，全部复用单实例 mutex 且原样传播退出码；`start-game-route.cmd` 已切到完整外循环，新增 `start-warehouse-cleanup.cmd` 用于单独验证两次仓库点击。
- 独立复审发现 entry/match 的 backend/max-hold 可漂移，以及 DryRun 被动回厅仍创建具备输入能力的 gateway；均已先红后绿修复。回厅现在使用只读 `Win32WindowProbe`，没有 send/key/mouse 方法，并从同一 HWND 派生标题与客户区。
- 本机使用真实 1920×1080 仓库 v3 bundle 执行 `external_session --cleanup-only --validate-only` 通过，输出精确标题、Profile ID、1920×1080 和 `armed_ready=false`，未创建运行 artifact 或输入对象。
- 本机 Python 3.12.12 全量验证：816 项测试通过，总覆盖率 85.07%；Ruff、sdist/wheel build、CLI help 与 `git diff --check` 通过。独立 code-reviewer 重放两项 HIGH 后最终无代码 finding，结论 APPROVE。
- Windows `192.168.3.7` publickey-only SSH 仍在 5 秒连接阶段超时；PowerShell parser、CMD 分支、0/1/2 退出码、仓库 live DryRun/Armed 和完整游戏 E2E 仍是交付门禁，本阶段没有发送任何真实游戏输入，也没有翻转 feature pass。

[2026-07-19 1920×1080 受保护路线采集]
- 新增 `route_capture_config.py`、`route_capture.py` 和 `route_capture_windows.py`：从 schema v2 配置加载确定性短脉冲计划，绑定一次精确 HWND、完整标题和初始 1920×1080 客户区；三帧确认 `IN_MATCH` 后才允许每步执行 pre-frame → 输入 → post-frame 事务。
- `start-route-calibration.cmd` 现提供 D/A/M/Q：D 只读确认 HUD，A 经 `.cmd`、PowerShell、Python 和 `route_capture.armed_ready` 四层授权执行短脉冲，M 保留菜单自动进图后的 120 秒人工只读采样。旧 M 分支不再被无关的 `game-route.json` 门禁阻断。
- 路线采集强制 F12=123，单脉冲与独立 Win32 watchdog 上限固定为 150ms，并在按键持有和 settle 期间每 10–50ms 复核 F12、前台 HWND/标题、客户区位置和尺寸。阻塞输入事件持久化时，独立 watchdog 仍可释放按键。
- 每一步必须先把 pre-frame 写盘；鼠标、key-down、部分或完整 key-up 都即时写入独立输入事件流；post-frame 必须晚于最后实际输入。输入发出后的步骤不重试。
- `run.json` 改为最终输入清理、截图源关闭和完成摘要全部成功后的最后提交。completed summary、最终输入释放或 source.close 任一故障时均撤销完整 marker，只生成 `partial-run.json` 和 failed summary。
- 本机真实 1080p 菜单 Profile 执行 `route_capture --validate-only` 通过：Profile `menu-zero-cost-1080p-v1`，客户区 1920×1080，实际 watchdog 150ms，计划 SHA-256 `94026f1bc406eea8ef1a67241661d2fdcfd608f91bf7ef7e6174762eaca37271`；校验没有创建运行 artifact 或输入 gateway。
- 本机 Python 3.12.12 全量验证：853 项测试通过，总覆盖率 84.77%；Ruff、`uv lock --check`、sdist/wheel build、`pip-audit` 与 `git diff --check` 通过。独立 code-reviewer 重放 F12、部分释放和完整 marker 三类 HIGH，修复后再复核 D/A/M 控制流与 README，最终无 CRITICAL/HIGH/MEDIUM/LOW finding，结论 APPROVE。
- 当前仍未在 Windows 执行 RouteCaptureDryRun/Armed；零号大坝真实移动路线 Profile、60 秒采集门槛、受控窗口 20/20、F12 实机 <=200ms、仓库真实点击和完整外循环 E2E 继续未完成，相关 feature passes 保持 false。
