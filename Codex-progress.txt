Shadow Runner Lab 进度归档
更新时间：2026-07-15
最新功能代码基线：434a697 fix: preserve mobile map breakpoints
GitHub：https://github.com/Lvjinhong/delta-shadow-runner

[已完成]
- Task 1：项目基线、统一 npm 脚本、TypeScript/Vite/Vitest 配置和 Windows 启动脚本。
- Task 2：A* 路径规划；19 项测试通过，并用 2,000 个随机图与参考 Dijkstra 对照通过。
- Task 3：模拟场景与恢复引擎；25 项状态机测试通过，覆盖定位、导航、卡住恢复、暂停、重置、乱序观测、能力门控和撤离。
- Task 4：REST/WebSocket 遥测服务；22 项服务端测试通过，API envelope、capabilities、timer、WebSocket 和并发关闭已加固。
- Task 5：工业战术 Web 管理端完成；包含 SVG 路线图、四项控制、实时指标、事件时间线、有限重连和显式安全边界。
- Web 侧完成协议握手保护、控制单 POST、卸载保护、任意坐标归一化、无碰撞 edge ID、对比度和读屏优化。
- Task 6：Windows 集成和非浏览器实机验证完成。
- Task 7：Windows Chromium E2E 门完成；真实 production bundle、REST、WebSocket 与浏览器交互 4/4 通过，无 retry、skip 或 mock。
- E2E 控制闭环覆盖启动、暂停、继续、注入卡住、recovering 遥测、恢复次数、最终撤离和 UI 重置；四项控制均通过键盘触发。
- 视觉与可访问性检查覆盖 1440×900、1024×768、390×844，验证无横向溢出、grid 断点、44px 控件、ARIA、唯一 ID 和 reduced motion。
- E2E 红灯发现并修复移动端地图断点被后加载 map.css 覆盖的问题；响应式高度与 map coordinates 隐藏规则已移到 map.css 文件末尾。
- Windows bootstrap 使用 SHA256 stamp + npm ls 完整性判断；重复运行跳过 npm ci，依赖漂移时自动重装。
- Windows PowerShell 5.1 的 UTF-8 无 BOM ParserError 已用 TDD 修复；bootstrap 测试增加到 6 项。
- 本地完整验证：7 个测试文件、118/118 测试、typecheck、server+web build、diff check 全部通过。
- Windows 已安装 Playwright Chromium 149.0.7827.55；JUnit 记录 tests=4、failures=0、errors=0、skipped=0。
- 独立 evaluator 在同一 Windows HEAD 再次只读复跑 4/4 通过，并复核报告时间、4 张成功截图、端口与进程清理。

[Windows 检查点]
- 路径：D:\Work\Work-Project\delta-shadow-runner
- 功能验证基线为 434a697；Windows main 与交付归档已同步且工作区干净，Node v24.14.0、npm 11.9.0。
- 实测首次安装、重复跳过、stamp 损坏、package-lock 漂移、npm ls 损坏和 ForceInstall 六条路径；每次测试均为 118/118。
- Windows typecheck、server build 和 Vite Web build 通过，npm ci audit 为 0 漏洞。
- 编译产物 smoke 通过：静态首页、health、snapshot、start/inject-stuck/pause/reset、非法控制 400、WebSocket connection/idle/localizing/recovering 和 SIGTERM 优雅关闭。
- 清理验证通过：临时文件不存在、端口 4199 无监听、stamp 匹配 lock、npm ls 返回 0。
- E2E 清理验证通过：端口 4173 无监听，没有命令行指向 ms-playwright 或 compiled server 的残留进程。
- 本机 PowerShell ExecutionPolicy 各作用域均为 Undefined，直接 .\init.ps1 会按默认 Restricted 被拒；使用 process-only Bypass，不修改持久策略。
- Windows 已配置 GitHub origin，但 Windows 自己尚未信任 GitHub Host Key；本轮代码通过已验证 Git bundle 经 win-dev SSH 同步。

[后续规划]
- 当前实现、Windows 集成和浏览器 E2E 均已完成；后续新增需求继续沿用模拟器/离线数据安全边界。
- Windows 直接从 GitHub fetch 仍需用户确认并录入 GitHub Host Key；当前同步链路继续使用已验证 Git bundle + win-dev SSH。
- GitHub 当前仓库实际为 public；如需 private，应由用户明确确认后再变更可见性。

[恢复开发]
- 本机：git clone git@github.com:Lvjinhong/delta-shadow-runner.git
- Windows：进入 D:\Work\Work-Project\delta-shadow-runner。
- 测试：powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\init.ps1 -Mode test
- 构建：powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\init.ps1 -Mode build
- 继续时先重读 docs/superpowers/specs/2026-07-14-shadow-runner-design.md 和 docs/superpowers/plans/2026-07-14-shadow-runner-implementation.md。

[安全边界]
- 仅使用模拟器、离线回放或 mock 数据。
- 不读取在线游戏进程，不发送真实键鼠输入，不包含反作弊绕过能力。
